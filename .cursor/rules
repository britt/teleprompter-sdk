# Cursor Agent Rules

- When changes are made, suggest any helpful updates to `AGENTS.md` and this file to keep future modifications consistent.
- GitHub Actions automatically run `npm test` on pull requests.
